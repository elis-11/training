const arr = [3, 4, 6];
const resPow = arr.map((a) => Math.pow(a, 3));
console.log(resPow);

const mult = arr.map((a) => a * 4);
console.log(mult);

const nums = [1, 4, 9];
const resSqrt = nums.map((num) => Math.sqrt(num));
console.log(resSqrt);
