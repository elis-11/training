const repeatStr= (n, s)=> {
    return s.repeat(n)
}
console.log(n=4, s='r');
// 2 - version
const repStr = (n, s)=> s.repeat(n)

const sum=(nums)=> nums.reduce((a,  b) => a + b, 0)
// console.log(sum(repStr));